apiVersion: v1
kind: Service
metadata:
  labels:
    app: httpbin
    role: green
    env: prod
  name: httpbin
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: httpbin
    role: green