apiVersion: v1
kind: Service
metadata:
  labels:
    app: httpbin
    role: test-blue
  name: httpbin-blue
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: httpbin
    role: blue


